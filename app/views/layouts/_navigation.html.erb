<nav class="navbar-default navbar-static-side" role="navigation">
  <div class="sidebar-collapse">
    <ul class="nav metismenu" id="side-menu">
      <li class="nav-header">
        <div class="dropdown profile-element">
          <a data-toggle="dropdown" class="dropdown-toggle" href="#">
            <span class="clear">
              <span class="block m-t-xs">
                <% if user_signed_in? %>
                  <strong class="font-bold"><%= current_user.first_name%></strong><br/>
                  <strong class="text-muted text-xs"><%= current_user.company_name%></strong>
                <% end %>
              </span> <span class="text-muted text-xs block">Menu<b class="caret"></b></span>
            </span>
          </a>
          <ul class="dropdown-menu animated fadeInRight m-t-xs">
             <li><%= link_to 'Profile', edit_user_registration_path %></li>
              <li><%= link_to 'Log out', destroy_user_session_path, method: :delete %></li>
          </ul>
        </div>
        <div class="logo-element">
          Oshie
        </div>
      </li>

      <!-- landing profile -->
      <li class="<%= 'active' if current_page?('/home/index') %>">
        <%= link_to "/home/index" do %>
          <i class="fa fa-home"></i> <span class="nav-label">Home</span>
        <% end %>
      </li>

      <!-- Classes -->
      <li class="<%= is_active_controller('classes') %>">
          <a href=""><i class="fa fa-users"></i>
            <span class="nav-label" data-i18n="nav.dashboard">Classes</span>
            <span class="fa arrow"></span></a>
          <ul class="nav nav-second-level collapse <%= is_active_controller('classes') %>">
              <li class="<%= is_active_action('my_classes') %>">
              <%= link_to "My Classes", classes_myClasses_path %></li>
              <li class="<%= is_active_action('new_class') %>">
              <%= link_to "Create New Class", classes_newClass_path %></li>

          </ul>
      </li>

      <!-- Evaluations -->
      <li class="<%= is_active_controller('evaluations') %>">
          <a href=""><i class="fa fa-pencil-square-o"></i>
            <span class="nav-label" data-i18n="nav.dashboard">Evaluations</span>
            <span class="fa arrow"></span></a>
          <ul class="nav nav-second-level collapse <%= is_active_controller('evaluations') %>">
              <li class="<%= is_active_action('my_evaluations') %>">
              <%= link_to "My Evaluations", evaluations_myEvaluations_path %></li>
          </ul>
      </li>


    </ul>
  </div>
</nav>
