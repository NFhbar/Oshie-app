<nav class="navbar-default navbar-static-side" role="navigation">
  <div class="sidebar-collapse">
    <ul class="nav metismenu" id="side-menu">
      <li class="nav-header">
        <div class="dropdown profile-element">
          <span>
              <img alt="image" class="img-circle" src="<%= image_url 'profile_small.jpg' %>"/>
          </span>
          <a data-toggle="dropdown" class="dropdown-toggle" href="#">
            <span class="clear">
              <span class="block m-t-xs">
                <% if user_signed_in? %>
                  <strong class="font-bold"><%= current_user.first_name%></strong><br/>
                  <strong class="text-muted text-xs"><%= current_user.company_name%></strong>
                <% end %>
              </span> <span class="text-muted text-xs block">Menu<b class="caret"></b></span>
            </span>
          </a>
          <ul class="dropdown-menu animated fadeInRight m-t-xs">
             <li><%= link_to 'Profile', edit_user_registration_path %></li>
              <li><%= link_to 'Log out', destroy_user_session_path, method: :delete %></li>
          </ul>
        </div>
        <div class="logo-element">
          Oshie
        </div>
      </li>

      <!-- landing profile -->
      <li class="<%= 'active' if current_page?('/home/minor') %>">
        <%= link_to "/home/minor" do %>
          <i class="fa fa-home"></i> <span class="nav-label">Home</span>
        <% end %>
      </li>

      <!-- Classes -->
      <li class="<%= is_active_controller('classes') %>">
          <a href=""><i class="fa fa-users"></i>
            <span class="nav-label" data-i18n="nav.dashboard">Classes</span>
            <span class="fa arrow"></span></a>
          <ul class="nav nav-second-level collapse <%= is_active_controller('classes') %>">
              <li class="<%= is_active_action('my_classes') %>">
              <%= link_to "My Classes", classes_myClasses_path %></li>
              <li class="<%= is_active_action('new_class') %>">
              <%= link_to "Create New Class", classes_newClass_path %></li>

          </ul>
      </li>

      <!-- Cognitive Stats -->
      <li class="<%= 'active' if current_page?('/cognitive/stats') %>">
        <%= link_to "/cognitive/stats" do %>
          <i class="fa fa-line-chart"></i> <span class="nav-label">Cognitive Stats</span>
          <span class="label label-primary pull-right">NEW</span>
        <% end %>
      </li>

      <!-- Evaluations -->
      <li class="<%= is_active_controller('evaluations') %>">
          <a href=""><i class="fa fa-pencil-square-o"></i>
            <span class="nav-label" data-i18n="nav.dashboard">Evaluations</span>
            <span class="fa arrow"></span></a>
          <ul class="nav nav-second-level collapse <%= is_active_controller('evaluations') %>">
              <li class="<%= is_active_action('my_evaluations') %>">
              <%= link_to "My Evaluations", evaluations_myEvaluations_path %></li>
              <li class="<%= is_active_action('new_eval') %>">
              <%= link_to "New Evaluation", evaluations_newEvaluation_path %></li>
          </ul>
      </li>

      <!-- Lessons -->
      <li class="<%= is_active_controller('lessons') %>">
          <a href=""><i class="fa fa-list-alt"></i>
            <span class="nav-label" data-i18n="nav.dashboard">Lessons</span>
            <span class="fa arrow"></span></a>
          <ul class="nav nav-second-level collapse <%= is_active_controller('lessons') %>">
              <li class="<%= is_active_action('my_lessons') %>">
              <%= link_to "My Lessons", lessons_myLessons_path %></li>
              <li class="<%= is_active_action('lesson_details') %>">
              <%= link_to "New Lesson", lessons_lesson_path %></li>
          </ul>
      </li>

      <!-- My Rewards -->
      <li class="<%= 'active' if current_page?('/rewards/myRewards') %>">
        <%= link_to "/rewards/myRewards" do %>
          <i class="fa fa-gift"></i> <span class="nav-label">My Rewards</span>
          <span class="label label-primary pull-right">NEW</span>
        <% end %>
      </li>

      <!-- Calendar -->
      <li class="<%= 'active' if current_page?('/calendar/myCalendar') %>">
        <%= link_to "/calendar/myCalendar" do %>
          <i class="fa fa-calendar"></i> <span class="nav-label">Calendar</span>
        <% end %>
      </li>

      <!-- Open School -->
      <li class="<%= 'active' if current_page?('/openschool/content') %>">
        <%= link_to "/openschool/content" do %>
          <i class="fa fa-university"></i> <span class="nav-label">Open School</span>
        <% end %>
      </li>

      <!-- Forum -->
      <li class="<%= 'active' if current_page?('/forum/forums') %>">
        <%= link_to "/forum/forums" do %>
          <i class="fa fa-commenting-o"></i> <span class="nav-label">Forums</span>
        <% end %>
      </li>


    </ul>
  </div>
</nav>
