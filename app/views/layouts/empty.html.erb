<!DOCTYPE html>
<html>
<head>
    <%= render 'layouts/meta' %>
    <title><%= full_title(yield(:title)) %></title>
    <%= render 'layouts/rails_default' %>
    <%= render 'layouts/shim' %>

</head>
<body class="gray-bg">

    <!-- Main view  -->
    <%= yield %>
    <%= debug(params) if Rails.env.development? %>

    <%= yield :javascript %>

</body>
</html>
